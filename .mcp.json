{
  "mcpServers": {
    "prequel": {
      "type": "http",
      "url": "https://mcp-beta.prequel.dev",
      "headers": {
        "Authorization": "Bearer ${user_config.jwt_token}"
      }
    }
  }
}
