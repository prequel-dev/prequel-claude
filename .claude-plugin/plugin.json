{
  "name": "prequel",
  "version": "0.1.0",
  "description": "Work with the Prequel MCP server over HTTP with JWT authentication.",
  "author": {
    "name": "Aleksandr Maus",
    "email": "aleks@prequel.dev"
  },
  "keywords": ["prequel", "mcp", "http", "jwt"],
  "userConfig": {
    "jwt_token": {
      "type": "string",
      "title": "JWT Bearer Token",
      "description": "Bearer JWT token used to authenticate against the Prequel MCP server (https://mcp-beta.prequel.dev). Sent as the Authorization header.",
      "sensitive": true,
      "required": true
    }
  }
}
